# Optional: LocalStack S3 (docker compose --profile localstack up -d).
# Get a token at https://localstack.cloud (free tier for individuals). Copy this file to .env in repo root.
LOCALSTACK_AUTH_TOKEN=